The create new project page has been removed. When a new user logs in for the first time and has no planner, one is automatically created and the user is redirected to it. There is no prompt — every user always needs exactly one planner to get started, so the intermediate step added unnecessary friction.

Creating additional planners is handled in the Multiple Planners feature via Settings.
