Each user should have a name associated with their account.

They should be prompted to enter a name after creating an account.

They should be able to edit the name in settings.

Update existing users with "New User" or some placeholder

The name should be a single string - we don't need their legal name. This is an account alias.