---
type: feature
status: idea
blocked-by: []
reviewed: 2026-09-25
---
# Where It Stands

Next: design session in Claude Design, then /assess. ^status

Questions for the design session:
- What counts as "empty space" in a cell that already has meal cards and a "+N more" link? What does each click target do?
- What cue shows that a cell can be clicked (hover state, cursor, a "+" that appears)?
- What's the keyboard equivalent? Today, Enter on a focused day opens its meal (one meal) or steps into its meals (several), and does nothing on an empty day. [[Keyboard Shortcuts]] also plans `C` to open Add Meal.
- Does clicking a day in the leading or trailing weeks of another month behave the same way?

# Purpose
add meals by clicking month cell

# Requirements
- On desktop month view, clicking a day cell opens the Add Meal modal with that day's date filled in.

# Out of Scope
- Mobile month view. Tapping a cell already selects the day, and the floating Add Meal button adds a meal on the selected day.
- Week view. "add meals by clicking week day" is its own Roadmap line. That story should reuse the interaction decisions from this story's design (click targets, hover cue, keyboard) so the two views behave the same.

## Meta-Instructions
%% Keep this section only if the note has a Design Handoff. Delete it otherwise. %%
The design handoff below is generated by Claude Design. It has been reviewed and approved by the user, but it is very detailed and the user may forget details in it (or she might change her mind). If there is a conflict between the design handoff and the user's instructions, you should prompt the user for clarification.

The design handoff should be treated as a source of truth (except in the case of user conflict) for the UX/UI. It should not be a source of truth for implementation details. Mantine boundaries are a best guess, not a binding contract. Clean architecture and component design are more important than trying to preserve the design handoff's approach.

# Design Handoff
%% Paste the Claude Design handoff here. Put images in `assets/<story-name>/` and embed them. Delete this section if there is no design. %%

# Suggested Approach
%% The approved technical approach: what behaviors the story covers and the key decisions (reuse vs. build new, client vs. server). Written during planning. %%

# Implementation
%% The step plan, once approved - then set status to `ready`. Each step should be small enough to review on its own, with acceptance criteria you can check in the running app. %%
