---
type: bug
status: spec
blocked-by: []
reviewed: 2026-09-25
---
# Where It Stands

Split from [[Unified Date Picker Component]] with 3 draft steps. Next: /plan-steps to confirm them. ^status

# Symptoms
- The header's Date field (desktop) and the period-label picker (phone) render unstyled, because nothing imports `@mantine/dates/styles.css`.
- Their weeks start on Monday, Mantine's default. Every week in the app starts on Sunday.
- Their height changes from month to month, because the grid isn't fixed at 6 rows.

Found by reading code during `/assess` of [[Unified Date Picker Component]].

# Who Can Hit This
Everyone, editors and viewers, on desktop and phone.

# Root Cause
- `src/app/layout.tsx` doesn't import `@mantine/dates/styles.css` and has no `DatesProvider`, so every Mantine date component falls back to its defaults: a Monday week start and a grid whose height varies.
- The week-start math is copied inline in `getWeekDates`, `getMonthGridDates` and `formatWeekRange`, with nothing tying it to the week start Mantine uses. Luxon's `startOf('week')` starts on Monday.

# Out of Scope
- Month views fixed at 6 rows are on the [[Roadmap#Calendar]].
- The dead `getWeekStart.ts` / `useWeekNavigation` are left for [[Remove Schedule-X]], which already lists them.

## Meta-Instructions
The design handoff below is generated by Claude Design. It has been reviewed and approved by the user, but it is very detailed and the user may forget details in it (or she might change her mind). If there is a conflict between the design handoff and the user's instructions, you should prompt the user for clarification.

The design handoff should be treated as a source of truth (except in the case of user conflict) for the UX/UI. It should not be a source of truth for implementation details. The Mantine boundaries and recommended configuration is a best guess not a binding contract. Clean architecture and component design are more important than trying to preserve the design handoff's approach.

# Design Handoff
The design lives in [[Unified Date Picker Component]]. The sections embedded below are part of this note.

![[Unified Date Picker Component#Mantine Boundary]]

![[Unified Date Picker Component#Behavior (all placements)]]

# Suggested Approach
*Approved by Sarah 2026-09-25 via `/assess` as part of [[Unified Date Picker Component]], and split into this story the same day. Behavior and piece numbers are the original story's, so gaps are behaviors or pieces that belong to other stories.*

## Behaviors
2. *(part)* Every Mantine date picker always shows 6 rows, so paging through months never changes its height. *(The rest of Behavior 2, the month arrows, is in [[Header Date Picker]].)*

*(Behavior 3 is in [[Header Date Picker]].)*

4. Every week in the app starts on **Sunday**: pickers, month grid, week view and date utilities. Monday-start code is tech debt.

## Pieces
| # | Piece | Job | Decision | Existing code | Why |
|---|---|---|---|---|---|
| 1 | App dates settings | Set the Sunday week start and 6-week grid for every Mantine date component | Use as-is (add to it) | `src/app/layout.tsx` | Setting up app-wide providers is already the layout's job. Add `DatesProvider` (`firstDayOfWeek` from the shared constant in row 2, `consistentWeeks: true`). Also add the missing `@mantine/dates/styles.css` import: nothing imports it today, so the current header pickers render unstyled. |
| 2 | Week-start utility | Return the Sunday that starts a date's week | Build new, then refactor callers | New: `getWeekStart` + `WEEK_START` constant in `src/_utils` (luxon). Refactor: `getWeekDates`, `getMonthGridDates`, `formatWeekRange` to call it. `weekdays.ts`: as-is. | Replaces three inline copies of the week-start math, and one constant feeds both Mantine and luxon. Never use luxon's `startOf('week')`, which starts on Monday. The dead `getWeekStart.ts` / `useWeekNavigation` are left for [[Remove Schedule-X]], which already lists them. |

These stay on the server: `layout.tsx` (with `DatesProvider`) and the week-start utility.

# Acceptance Criteria
- [ ] The header's Date field (desktop) and the period-label picker (phone) show a styled calendar grid.
- [ ] Their weeks start on Sunday.
- [ ] They always show 6 rows, so their height doesn't change between months.
- [ ] The month and week views look and work exactly as before.

# Implementation
%% A small bug may need only one step, but it still goes here so it can be reviewed. Once it exists, set status to `ready`. %%
