---
type: feature
status: spec
blocked-by:
  - "[[Mantine Date Picker Setup]]"
  - "[[Today and Selected Day Markers]]"
confirmed: 2026-09-25
---
# Where It Stands

Split from [[Unified Date Picker Component]] with 11 draft steps. Next: /plan-steps to confirm them. Building waits on [[Mantine Date Picker Setup]] and [[Today and Selected Day Markers]]. ^status

# Purpose
The calendar header has two separate jump-to-date pickers, one for desktop and one for phones, and neither matches the design. This story builds the one shared `CalendarDatePicker` panel, with its desktop popover and phone bottom sheet, and moves both header variants onto it. The meal form ([[Meal Form Date Picker]]) and Move to / Duplicate ([[Meal Detail Modal & Edit Meals]]) reuse it later.

# Requirements
- Everything in the embedded design sections below that applies to the picker panel, its popover and bottom sheet, and the header placement.

# Out of Scope
- The meal form placement belongs to [[Meal Form Date Picker]].
- The picker's `label`, relative shortcuts, custom `shortcutItems` and `hint` moved to [[Meal Editing#Date picker options for Move / Duplicate]].
- The Move to / Duplicate placement belongs to [[Meal Detail Modal & Edit Meals]].

## Meta-Instructions
The design handoff below is generated by Claude Design. It has been reviewed and approved by the user, but it is very detailed and the user may forget details in it (or she might change her mind). If there is a conflict between the design handoff and the user's instructions, you should prompt the user for clarification.

The design handoff should be treated as a source of truth (except in the case of user conflict) for the UX/UI. It should not be a source of truth for implementation details. The Mantine boundaries and recommended configuration is a best guess not a binding contract. Clean architecture and component design are more important than trying to preserve the design handoff's approach.

# Design Handoff
The design lives in [[Unified Date Picker Component]]. The sections embedded below are part of this note.

![[Unified Date Picker Component#^overview]]

![[datepicker_placements.png]]

![[Unified Date Picker Component#One panel, three placements]]

![[datepicker_anatomy.png]]

![[Unified Date Picker Component#^anatomy-parts]]

![[Unified Date Picker Component#Day cell states]]

![[Unified Date Picker Component#Behavior (all placements)]]

![[Unified Date Picker Component#Mantine Boundary]]

![[Unified Date Picker Component#Recommended Configuration]]

![[Unified Date Picker Component#Phones Bottom Sheet]]

![[Unified Date Picker Component#Aligning every today / selected indicator]]

![[Unified Date Picker Component#Files]]

# Suggested Approach
*Approved by Sarah 2026-09-25 via `/assess` as part of [[Unified Date Picker Component]], and split into this story the same day. Behavior and piece numbers are the original story's, so gaps are behaviors or pieces that belong to other stories.*

## Behaviors
**Picker panel (every placement)**
1. Opens on the month of the current value, or on today's month if there's no value.
2. Previous and next arrows page by month. The grid is always 6 rows, so the panel height never changes.
3. Clicking the month title opens the month and year views.

*(Behavior 4, the Sunday week start, is built by [[Mantine Date Picker Setup]].)*

5. Day cell states: default (navy), outside month (`navy.3`, still pickable), has meals (forest dot), today (navy ring, bold), selected (sage circle). Today and selected can stack.
6. Picking a day from a neighboring month selects it and moves the view to that month.
7. Past dates can be picked.
8. Keyboard: arrows move by day or week, PgUp/PgDn by month, Home/End to the start or end of the week, Enter picks, Esc closes.

*(Behavior 9, the Today footer, is in [[Meal Form Date Picker]].)*

10. The panel only reports the pick. The placement closes it and acts on the date.
11. A day with at least one meal gets a dot. Any meal counts, and titles aren't parsed. The meal dates come from the calendar data the page already loads on the server, so there's no fetching, loading state or error state.

**Desktop and phone containers**

12. On desktop it's a popover anchored to its trigger. Mantine flips it above when there's no room below. *(Changed 2026-09-25 when the story was split: this is Mantine's default, so it gets no check of its own.)*
13. On phones it's a bottom sheet, not full screen. It has a title and a Close button, and swiping down on its handle or header, or tapping the backdrop, closes it. It clears the safe area and scrolls if it gets taller than 90% of the screen.
    - **Sheet title:** **"Jump to date"**. *(The meal form's "Choose day" is in [[Meal Form Date Picker]]. A placement's `label` as the sheet title moved to [[Meal Editing#Date picker options for Move / Duplicate]].)*
14. On phones it uses touch sizes: day cells around 50px, 44px arrows, 17px numbers, bigger dots. Pressed states replace hover.
15. On phones, swiping left or right on the grid pages months.

**Header placement**

16. On desktop the trigger is an input-styled button labeled "Date", showing the date in view as `9/24/2026`.
    - **Phones:** the period label ("September 2026 ▾") stays the trigger, as the approved design in the archived [[Mobile Month View]] note specifies.
    - **The picker:** marks the date in view as selected, shows meal dots and has no Today footer.
17. Picking a date moves the calendar to that date and closes the picker.

**Read-only users**

28. Viewers use the header picker like everyone else, and nothing behaves differently for them. They never see the meal form.

## Pieces
| # | Piece | Job | Decision | Existing code | Why |
|---|---|---|---|---|---|
| 3 | Picker panel | Lay out the optional parts (label, shortcuts, hint, Today footer) around the day grid | Build new (`src/_components`) | — | There's nothing to extract. |
| 4 | Picker grid | Show a month grid with today, selected and marked-day states | Build new | Header `DatePicker`: not a fit | Wraps Mantine `DatePicker`. Only the header's chevron icons carry over. |
| 8 | Swipe hook | Report swipe direction (left, right or down) on an element | Build new in `src/_hooks` | — | `@mantine/hooks` has no swipe hook. |
| 10 | Adaptive popover | Show content in a popover on desktop, or in a bottom sheet on phones | Build new (generic, `src/_components`) | `useIsMobile`: as-is. `CalendarModalProvider`: not a fit, untouched | Mantine `Popover` on desktop and `Drawer position="bottom"` on phones. This goes beyond the handoff, which has each placement own its popover/drawer. That would copy the desktop/phone switch into every placement, the drift this story exists to stop. A downward swipe on the sheet's handle or header closes it; swipes inside the content never do, so they can't clash with scrolling or month paging (decided by Sarah 2026-09-25 during `/plan-steps`). |
| 11 | Date trigger | Show a date in an input-styled button | Build new on `InputBase component="button" pointer` | Header `DatePickerInput` and the native date input: replace | Mantine first. |
| 12 | Date formatting | Format an ISO date in a named style | Build new `formatDate(iso, style)` in `src/_utils` (luxon) | `src/_utils/date.ts`: not a fit, left alone | `date.ts` parses date-only strings as UTC and shows the previous day in US time zones. Also move the duplicated format strings onto named styles: `AddMealForm:40`, `MobileAgenda:46-47`, `Calendar/WeekView:73`, `formatWeekRange`. |
| 13 | Meal dates | List the dates that have at least one meal | Build new in `src/app/[planner]/calendar/_utils`, computed in `page.tsx` on the server | `toCalendarMeals` / `toCalendarEvents` / `toScheduleXEvents`: not a fit | Built straight from `SerializedDay[]`. The types are imported from where they are now. **Flag:** if [[Shared Types Directory]] or [[Remove Schedule-X]] lands first, import them from wherever that story puts them and follow its pattern. |
| 14 | Meal dates provider | Provide the planner's meal dates to the calendar page's components | Build new: `MealDatesProvider`, placed by `CalendarView` | `CalendarView`: small wiring change. `CalendarProvider`, `CalendarModalProvider`, `AddMealModal`, `AddMealForm`: untouched | Avoids passing a prop through 4 modules and keeps meal data out of generic navigation state. The data arrives as server props, which fits [[Stale Data Issues]] Rule 2. |
| 15 | Jump to date | Move the calendar to a picked date | Build new, extracted from the header | `CalendarHeader`: refactor first (remove both jump implementations; both variants render `JumpToDate`) | Jump-to-date is built twice today. Desktop uses the input-styled Date field. Phones keep the period label as the trigger. |
| 20 | Picker keys | Move picker focus with PgUp/PgDn (same day, previous/next month) and Home/End (start/end of the week) | Build new, next to the picker: a `useDatePickerKeys` hook, plus a pure `getKeyTargetDate(date, key)` for the date math | Mantine `DatePicker`: handles arrows and Ctrl+arrows only | *Added 2026-09-25 by Sarah during `/plan-steps`.* Mantine has no PgUp/PgDn or Home/End. Kept picker-specific because it has one caller; the calendar grids' `useRovingGridFocus` is the likelier future sharer, not the global shortcuts. The date math sits in its own function so it's easy to test and to lift out later. `getKeyTargetDate` uses `getWeekStart` (row 2). |

Built by other stories:
- Piece 4 uses the Day marks ring and circle styles and the `sage` token from [[Today and Selected Day Markers]] (Pieces 9 and 18).
- Piece 20 uses `getWeekStart` from [[Mantine Date Picker Setup]] (Piece 2).
- Piece 12's `AddMealForm:40` moves in [[Meal Form Date Picker]].

## Client pieces
The panel (3) and trigger (11) have no `'use client'` directive of their own. Both have event handlers, but they only ever render inside the client pieces below.

| Piece | Why client |
|---|---|
| Picker grid (4) | Passes function props (`getDayProps`, `onChange`) to Mantine's `DatePicker`, and handles swipes |
| Swipe hook (8) | Touch events |
| Adaptive popover (10) | Uses a media query (`useIsMobile`) |
| Meal dates provider (14) | React context |
| Jump to date (15) | Holds the open state; reads the calendar context |
| Picker keys hook (20) | Keyboard events on the grid |

These stay on the server: meal dates computed in `page.tsx`, and all the utilities.

# Implementation
%% The step plan, once approved - then set status to `ready`. Each step should be small enough to review on its own, with acceptance criteria you can check in the running app. %%
