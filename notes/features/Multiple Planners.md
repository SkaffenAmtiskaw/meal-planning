1. ~~Add a name to planners - we can update existing planners with `{user.name}'s planner`~~ ([[Add Planner Settings]])
2. ~~Add tabs to the settings - one for user settings and one for planners~~ ([[Add Planner Settings]])
3. The planners tab should show a list of the user's planners, and allow the user to edit the name of the planner (more settings will come later) — ([[Add Planner Settings]] handles displaying the list; editing the name is part of this feature)
4. There should be a button to create a new planner on that page
5. Decision: Either a user should be able to designate a "primary" planner that loads on login, or the last planner they had open should be loaded. (Weigh pros/cons).