1. Add a name to planners - we can update existing planners with `{user.name}'s planner`
2. Add tabs to the settings - one for user settings and one for planners
3. The planners tab should show a list of the user's planners, and allow the user to edit the name of the planner (more settings will come later)
4. There should be a button to create a new planner on that page
5. Decision: Either a user should be able to designate a "primary" planner that loads on login, or the last planner they had open should be loaded. (Weigh pros/cons).