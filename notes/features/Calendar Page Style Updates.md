The FullCalendar component has been rendered in the calendar page.

It needs to be resized to fit better on the page. It also need to handle the sidebar opening/closing gracefully.

The header buttons and text also needs to be aligned with Mantine styles - it does not look like FullCalendar allows custom components for the header (although this should be confirmed) so we will probably have to use CSS for this - we should investigate if Mantine has CSS classes we can use.

