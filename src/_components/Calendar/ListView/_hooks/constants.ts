// Vertical gap (in px) left between the top of the scroll container and the
// scrolled-to day row. Also used as the detection threshold so the active day
// in the list matches the day shown in the date picker.
export const LIST_VIEW_TOP_PADDING = 12;
