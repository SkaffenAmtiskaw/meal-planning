const Page = () => {
	/**
	 * 1. If the user has a query param of "invite", make sure they are signed in then add that meal plan to their user.
	 * 2. If the user is signed in, check if they have an existing meal plan. If so, redirect them to /planner
	 * 3. If the user is signed in but does not have an existing meal plan, prompt them to create one.
	 * 4. If the user is not signed in, try to sign them in. Redirect them to sign-in if they are stubborn.
	 */

	return 'Hello world';
};

export default Page;
